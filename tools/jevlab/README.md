# tools/jevlab

| Script | What it does |
|---|---|
| protocol.json | Pre-registered grid, seeds, criteria, dataset pointers. The scorer reads this. |
| fetch_sst2.py | Freeze SST-2 train/valid/test plus the negation slice. |
