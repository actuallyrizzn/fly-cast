# Smoke slices only

Real corpora and GloVe live on ngram at ~/fly-cast-runs/jevlab/data and
~/fly-cast-runs/jevlab/vectors. This directory holds the small slices tests
and dry runs use. Do not commit the full downloads.
